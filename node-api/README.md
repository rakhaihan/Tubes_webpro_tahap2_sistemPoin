# Node API (Express)

Simple Express scaffold for the project.

Run locally:

```bash
cd node-api
npm install
npm run dev   # requires nodemon
```

API examples:
- GET /api/ping
- GET /api/pelanggaran
- GET /api/pelanggaran/:id

Port defaults to 3001 (see `.env.example`).
